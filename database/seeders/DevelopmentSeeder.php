<?php

namespace Database\Seeders;

use Illuminate\Database\Console\Seeds\WithoutModelEvents;
use Illuminate\Database\Seeder;

/**
 * Seeder responsável por popular o banco de dados com dados fictícios (factories).
 *
 * Observação:
 * Este seeder NÃO é executado automaticamente pelo DatabaseSeeder,
 * devendo ser chamado manualmente quando necessário, geralmente em
 * ambientes de desenvolvimento ou testes.
 *
 * Objetivo:
 * - Gerar dados fake para simular cenários reais da aplicação
 * - Auxiliar no desenvolvimento, testes e validações de regras de negócio
 *
 * Uso:
 * php artisan db:seed --class=DevelopmentSeeder
 */
class DevelopmentSeeder extends Seeder
{
    /**
     * Run the database seeds.
     */
    public function run(): void
    {

    }
}
